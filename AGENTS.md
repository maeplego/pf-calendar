# P05 pf-calendar

このディレクトリは製品コードです。実装前にワークスペース（`project`）側を読む。

1. `project/portfolio-plan/instructions.md` または `project/AGENTS.md`
2. `project/calendar/AGENTS.md`
3. `project/portfolio-plan/calendar/DESIGN.md`
4. `project/portfolio-idea/11-booking-scheduler.md`
5. `project/calendar/chat-context/` をファイル名順

テスト: `npm test` をリポジトリルートで実行する（`packages/slot-engine` と `apps/api`）。通ってからこのリポジトリにコミットする。
